export enum LoginStatus {
    ALREADY_LOGGED_IN,
    SUCCESSFUL_LOGIN,
    SUCCESSFUL_LOGOUT
}
export enum RegisterStatus {
    ALREADY_LOGGED_IN,
    REGISTERED
}
export enum SessionStatus {
    EXPIRED,
    NOT_LOGGED_IN
}